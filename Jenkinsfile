pipeline {

agent any
stages {

     stage('scm') {
            steps {
           echo "my git code download"
            }
}
     stage('deploy') {
            steps {
           echo "deploy the code"
            }
}
       
     stage('test') {
           echo "test my code"
           }
}

 
}
}
